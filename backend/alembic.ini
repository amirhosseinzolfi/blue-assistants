# A generic alembic.ini file.
# Lines starting with # or ; are comments

[alembic]
# path to migration scripts
script_location = alembic/versions

# template for migration file names
# file_template = %%(rev)s_%%(slug)s

# timezone for timestamps within migration files
# timezone = UTC

# system to use for file generation
# output_encoding = utf-8

sqlalchemy.url = sqlite:///./chat_platform.db

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
