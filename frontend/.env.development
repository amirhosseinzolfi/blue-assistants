# Frontend environment variables for development
# Prefix with VITE_ to expose them to the app

# Example: If your backend runs on a different port or host during development
# VITE_API_BASE_URL=http://localhost:8000/api/v1
